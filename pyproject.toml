[project]
name = "file-parsing"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pdfplumber",
    "python-docx",
    "openpyxl",
    "paddlepaddle",
    "paddleocr",
    "Pillow",
    "olefile>=0.47",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
